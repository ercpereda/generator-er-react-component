import React from 'react';
import ReactDOM from 'react-dom';
import <%= componentName %> from './<%= componentName %>';

ReactDOM.render(<<%= componentName %> text={'Hello World!!'} />, document.getElementById('app'));
